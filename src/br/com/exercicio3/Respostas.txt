Respostas da Atividade 03:

I:
public interface CalculadoraBasica {

    public float somar(float op1, float op2);

    public float subtrair(float op1, float op2);

    public float dividir(float op1, float op2);

    public float multiplicar(float op1, float op2);
}

public class CalculadoraBasicaImplements implements CalculadoraBasica {

    @Override
    public float somar(float op1, float op2) {
        return op1 + op2;
    }

    @Override
    public float subtrair(float op1, float op2) {
        return op1 - op2;
    }

    @Override
    public float dividir(float op1, float op2) {
        if(op2 == 0){
            System.out.println("Dado invalido na divisão");
            return 0;
        }else{
            return op1 / op2;
        }
    }

    @Override
    public float multiplicar(float op1, float op2) {
        return op1 * op2;
    }
    
}

public class Main01 {
    
    public static void main(String[] args) {
        try{
            Float op1 = Float.parseFloat(JOptionPane.showInputDialog("Digite o primeiro número: "));
            Float op2 = Float.parseFloat(JOptionPane.showInputDialog("Digite o segundo número: "));
            
            CalculadoraBasicaImplements cal = new CalculadoraBasicaImplements();
            System.out.println(cal.somar(op1, op2));
            System.out.println(cal.subtrair(op1, op2));
            System.out.println(cal.multiplicar(op1, op2));
            System.out.println(cal.dividir(op1, op2));
        }catch(Exception e){
            JOptionPane.showMessageDialog(null, "Você digitou um dado invalido.");
        }
    }
}

II:
a. objeto para equipamento; 		-- Conversão válida
b. objeto para movel; 				-- Conversão válida
c. equipamento para tablet2;		-- Conversão implícita
d. equipamento para smartphone2;	-- Conversão implícita
e. smartphone para tablet;			-- Erro de execução
f. smartphone2 para tablet2;		-- Erro de execução
g. equipamento para tablet;			-- Conversão válida
h. movel para tablet;				-- Casting válido
i. tablet para equipamento;			-- Erro de execução

III:
O Main da letra "a" está correto, pois a class ChowChow estância a class Cachorro, porém o inverso não ocorre (podendo existir 
até mesmo erro na aplicação caso não trate o trecho do código).

IV:
public class Main04 {
    public static void main(String[] args) {
        try{
            Float op1 = Float.parseFloat(JOptionPane.showInputDialog("Digite o primeiro número: "));
            Float op2 = Float.parseFloat(JOptionPane.showInputDialog("Digite o segundo número: "));
            
            CalculadoraBasicaImplements cal = new CalculadoraBasicaImplements();
            System.out.println(cal.somar(op1, op2));
            System.out.println(cal.subtrair(op1, op2));
            System.out.println(cal.multiplicar(op1, op2));
            System.out.println(cal.dividir(op1, op2));
        }catch(Exception e){
            JOptionPane.showMessageDialog(null, "Você digitou um dado invalido.");
        }
    }
    
    public static Double efetuarCalculo(Double numero1, Double numero2, String operador){
        if(operador.equals("+")){
            return numero1 + numero2;
        }else if(operador.equals("-")){
            return numero1 - numero2;
        }else if(operador.equalsIgnoreCase("x")){
            return numero1 * numero2;
        }else if(operador.equals("÷") || operador.equals("/")){
            if(numero2 == 0){
                return null;
            }else{
                return numero1 / numero2;
            }
        }else if(operador.equalsIgnoreCase("%")){
            return numero1 % numero2;
        }else{
            return null;
        }
    }
}

public class CalculadoraBasicaImplements implements CalculadoraBasica {

    @Override
    public float somar(float op1, float op2) {
        return op1 + op2;
    }

    @Override
    public float subtrair(float op1, float op2) {
        return op1 - op2;
    }

    @Override
    public float dividir(float op1, float op2) {
        if(op1 == 0){
            System.out.println("Dado invalido na divisão");
            return 0;
        }else{
            return op2 / op1;
        }
    }

    @Override
    public float multiplicar(float op1, float op2) {
        return op1 * op2;
    }
    
}


